spring:
  profiles:
    active: prod
#mysql配置
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/dbgirl
    username: root
    password:
#spring data-jpa配置,create 自动帮我们创建一个表
  jpa:
    hibernate:
      ddl-auto: update
#打印sql语句
    show-sql: true

